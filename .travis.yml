###########################
####### README FIRST! #####
###########################
# This file should only be edited in our main repository (code)
# You are doing something wrong
# if you are trying to edit this file in ci-prebuilds.
#
# Our automation is grabbing the version of this file from our main repository
# and commit it with automatic changes to ci-prebuilds
# and only when Conan dependencies are actually changed
# in order to not cause unnecessary rebuilds.
#
# The ci-prebuilds version will have un-comment every line starting with #CI(followed by an empty space)
# e.g. #CI echo this only gets executed in ci-prebuilds
#
# The ci-prebuilds version will have every line removed containing #CEDELETE
# e.g. #CIDELETE this line is not visible in ci-prebuilds aka. won't get executed
#

sudo: required
dist: trusty
language: cpp

services:
  - docker

git:
  depth: 15

jobs:
  include:
  # We run the following jobs in our main repository:    #CIDELETE
    - stage: Conan #CIDELETE
      os: linux #CIDELETE
      env: docker_tag="NODOCKER" conan_prebuilds="true" #CIDELETE
    - stage: Testing #CIDELETE
      os: linux #CIDELETE
      compiler: clang #CIDELETE
      env: os=linux docker_tag="optimization" TARGET=linux_clang CONAN_COMPILER="clang" CONAN_COMPILER_VERSION="4.0" CMAKE_CC="clang-4.0" CMAKE_CXX="clang++-4.0" NIGHTLY="true" #CIDELETE
    - os: linux #CIDELETE
      compiler: gcc #CIDELETE
      env: os=linux docker_tag="optimization" TARGET=linux_gcc CONAN_COMPILER="gcc" CONAN_COMPILER_VERSION="6.3" CMAKE_CC="gcc-6" CMAKE_CXX="g++-6" NIGHTLY="true" #CIDELETE
    #- os: linux #CIDELETE
      #sudo: required #CIDELETE
      #dist: trusty #CIDELETE
      #env: TARGET=apidoc #CIDELETE
      #git: #CIDELETE
        #submodules: false #CIDELETE

    # We run the following jobs in ci-prebuilds:
    # Our main compilers we run tests for
    #CI - os: linux
      #CI compiler: gcc
      #CI env: os=linux docker_tag="optimization" TARGET=linux_gcc CONAN_COMPILER="gcc" CONAN_COMPILER_VERSION="6.3" CMAKE_CC="gcc-6" CMAKE_CXX="g++-6" NIGHTLY="conan"

    #CI - os: linux
      #CI compiler: clang
      #CI env: os=linux docker_tag="optimization" TARGET=linux_clang CONAN_COMPILER="clang" CONAN_COMPILER_VERSION="4.0" CMAKE_CC="clang-4.0" CMAKE_CXX="clang++-4.0" NIGHTLY="conan"

    # Older compilers we compile Conan packages for, to speed things up for other people
    #CI - os: linux
      #CI compiler: gcc
      #CI env: os=linux docker_tag="legacy" TARGET=linux_gcc CONAN_COMPILER="gcc" CONAN_COMPILER_VERSION="5.4" CMAKE_CC="gcc-5" CMAKE_CXX="g++-5" NIGHTLY="conan"

    #CI - os: linux
      #CI compiler: clang
      #CI env: os=linux docker_tag="legacy" TARGET=linux_clang CONAN_COMPILER="clang" CONAN_COMPILER_VERSION="3.9" CMAKE_CC="clang-3.9" CMAKE_CXX="clang++-3.9" NIGHTLY="conan"

    #CI - os: linux
      #CI compiler: clang
      #CI env: os=linux docker_tag="legacy" TARGET=linux_clang CONAN_COMPILER="clang" CONAN_COMPILER_VERSION="3.8" CMAKE_CC="clang-3.8" CMAKE_CXX="clang++-3.8" NIGHTLY="conan"

    #- os: osx
      #sudo: false
      #osx_image: xcode8
      #compiler: clang
      #env: os=osx compiler=clang TARGET="osx" CMAKE_FLAGS="-DCMAKE_CXX_COMPILER=clang++-3.9 -DCMAKE_C_COMPILER=clang-3.9 -DBUILD_ALL=1"

  #CI allow_failures:
    #CI - env: os=linux docker_tag="legacy" TARGET=linux_gcc CONAN_COMPILER="gcc" CONAN_COMPILER_VERSION="5.4" CMAKE_CC="gcc-5" CMAKE_CXX="g++-5" NIGHTLY="conan"
    #CI - env: os=linux docker_tag="optimization" TARGET=linux_clang CONAN_COMPILER="clang" CONAN_COMPILER_VERSION="4.0" CMAKE_CC="clang-4.0" CMAKE_CXX="clang++-4.0" NIGHTLY="conan"
    #CI - env: os=linux docker_tag="legacy" TARGET=linux_clang CONAN_COMPILER="clang" CONAN_COMPILER_VERSION="3.8" CMAKE_CC="clang-3.8" CMAKE_CXX="clang++-3.8" NIGHTLY="conan"

  #CI fast_finish: true # don't wait for allow_failures to fail, gives faster response
    #- os: linux
      #sudo: required
      #dist: trusty
      #env: TARGET=apidoc
      #git:
        #submodules: false

  ## Old stuff:
  #- os: osx
      #sudo: false
      #osx_image: xcode8
      #compiler: clang
      #env: TARGET="osx" CMAKE_FLAGS="-DCMAKE_CXX_COMPILER=clang++-3.8 -DCMAKE_C_COMPILER=clang-3.8"


  #allow_failures:
    #- os: osx
      #sudo: false
      #osx_image: xcode8
      #compiler: clang
      #env: os=linux compiler=clang TARGET="osx" CMAKE_FLAGS="-DCMAKE_CXX_COMPILER=clang++-3.9 -DCMAKE_C_COMPILER=clang-3.9 -DBUILD_ALL=1"


before_install:
  # Get our CI Docker image
  - if ! [[ "${docker_tag}" == 'NODOCKER' ]]; then
        sudo docker pull inexorgame/ci-docker':'${docker_tag} ;
    fi

script:
  - if [ "${conan_prebuilds}" == "true" ]; then
      set -e ;
      bash ./tool/travis_conditional_conan_package_generating.sh ;
    else
      echo "${conan_prebuilds}" ;
    fi
  - if [ "${GLOBAL_NIGHTLY}" == "false" ]; then
      NIGHTLY="false";
    fi
  - if [ "${GLOBAL_NIGHTLY}" == "conan" ]; then
      NIGHTLY_USER="${CONAN_USER}";
      NIGHTLY_PASSWORD="${CONAN_PASSWORD}";
    else
      NIGHTLY_USER="${FTP_USER}";
      NIGHTLY_PASSWORD="${FTP_PASSWORD}";
    fi
  - if ! [[ "${docker_tag}" == "NODOCKER" ]]; then
    sudo docker run -v $(pwd)':'/inexor --net=host -it inexorgame/ci-docker':'${docker_tag} /inexor/${RELATIVE_PATH}tool/travis.sh target_script /${RELATIVE_PATH} ${TARGET} ${CONAN_COMPILER} ${CONAN_COMPILER_VERSION} ${CMAKE_CC} ${CMAKE_CXX} ${TRAVIS_COMMIT} ${TRAVIS_BRANCH} ${TRAVIS_JOB_NUMBER} ${NIGHTLY} ${NIGHTLY_USER} ${NIGHTLY_PASSWORD} ${FTP_DOMAIN};
    fi
  - if [ "${os}" == "osx" ]; then
      bash tool/travis.sh target_before_install;
    fi
